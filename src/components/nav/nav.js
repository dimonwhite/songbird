import React from 'react';
import './nav.scss';

const Nav = () => (
  <nav className="nav">
    <ul className="nav_list">
      <li className="nav_list_item">Сериалы</li>
    </ul>
  </nav>
);

export default Nav;
